# ───────────────
# Supabase (public)
# ───────────────
# URL del proyecto Supabase (Project Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co

# Clave pública (anon) para el cliente (Project Settings → API → anon public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# ───────────────
# Supabase (server-only, NO exponer en el cliente)
# ───────────────
# Clave service_role (solo para el backend, NUNCA usar como NEXT_PUBLIC_)
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY

# ───────────────
# Storage (bucket para imágenes del admin)
# ───────────────
STORAGE_BUCKET=products

# ───────────────
# Admin (solo DEV; opcional)
# Usuarios listados aquí entran al /admin aunque falte perfil en DB.
# En producción es mejor usar sólo perfiles/roles RLS.
# ───────────────
ADMIN_EMAILS=
ADMIN_ROLES=owner,editor

# ───────────────
# Nombres de tablas reales (tu esquema usa prefijo catalogo_)
# Cambiá si en el futuro modificás prefijos.
# ───────────────
DB_TABLE_PRODUCTS=catalogo_products
DB_TABLE_VARIANTS=catalogo_variants
DB_TABLE_IMAGES=catalogo_images
DB_TABLE_PROFILES=catalogo_profiles

# ───────────────
# SEO / URLs
# ───────────────
# URL pública del sitio (en dev suele ser http://localhost:3000)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
SITE_URL=http://localhost:3000

# ───────────────
# Contacto
# ───────────────
# Teléfono en formato internacional sin espacios.
# El código limpia símbolos, pero es buena práctica dejar sólo números.
WHATSAPP_PHONE=598XXXXXXXX

# ───────────────
# Tracking / UTM
# ───────────────
# Salt para hashear IPs en /w (puede ser cualquier string larga)
UTM_IP_SALT=some-random-salt
