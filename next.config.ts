import type { NextConfig } from "next";

const nextConfig = {
  images: {
    remotePatterns: [{ protocol: "https", hostname: "jfmwcehiqeejwyvmnmrq.supabase.co" }],
  },
};
export default nextConfig;

